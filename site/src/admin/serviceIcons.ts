// Mirrors server/src/models/Service.ts's SERVICE_ICON_KEYS — the admin
// Service form is restricted to this fixed list so authored content can
// only reference icons that actually exist in the public site's icon set.
export const SERVICE_ICON_OPTIONS = ['cap', 'person', 'pin', 'check', 'shield', 'clock', 'book']
