Place real images here (hero slides, about-page photos, candidate/school
photos, etc.) using the same filenames referenced in the theme when this
is converted to WordPress. Every image area on this site uses a
fixed-size CSS container, so dropping in real photos will not change any
page's layout.
